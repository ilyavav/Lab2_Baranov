info:
  name: Get Admin Token
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:7175/api/Auth/token/secret
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
